---
name: internal-sync

"on":
  schedule:
    - cron: "30 * * * *"
  workflow_dispatch:

permissions: {}

concurrency:
  group: gh-actions-glab-internal
  cancel-in-progress: false

jobs:
  sync:
    permissions:
      contents: read
    uses: ./.github/workflows/reconcile-target.yml
    with:
      mode: internal
    secrets:
      BWS_ACCESS_TOKEN: ${{ secrets.BWS_ACCESS_TOKEN }}
      BWS_PROJECT_ID: ${{ secrets.BWS_PROJECT_ID }}

  configure-mirrors:
    if: needs.sync.result == 'success' && needs.sync.outputs.has_mirror_targets == 'true'
    needs:
      - sync
    permissions:
      contents: read
    uses: ./.github/workflows/configure-target-mirrors.yml
    with:
      mode: internal
    secrets:
      BWS_ACCESS_TOKEN: ${{ secrets.BWS_ACCESS_TOKEN }}
      BWS_PROJECT_ID: ${{ secrets.BWS_PROJECT_ID }}
